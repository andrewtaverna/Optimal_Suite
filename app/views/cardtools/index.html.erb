<div class="container">
<br>
<br>

<div class="row">
	<div class="col-md-8">

	<div class="panel panel-default panel-table">
              <div class="panel-heading">
                <div class="row">
                  <div class="col col-xs-6">
                    <h3 class="panel-title">Card Sorting!</h3>
                  </div>
                  <div class="col col-xs-6 text-right">
                    <a href="/cardtools/new/" type="button" class="btn btn-sm btn-primary btn-create">Create a Campaign</a>
                  </div>
                </div>
              </div>
              <div class="panel-body">
                <table class="table table-striped table-bordered table-list">
                  <thead>
                    <tr>
                        <th><em class="fa fa-cog"></em></th>
                        <th>Name</th>
                        <th>Description</th>
                    </tr> 
                  </thead>
                  <tbody>

	  				<% @cardtools.each do |cardtool| %> 
					<% if can? :show, cardtool %>

					<tr>
                        <td align="center">
                          <a href="<%= cardtool_path(id: cardtool.id) %>" class="btn btn-default"><em class="fa fa-pencil"></em></a>
      					<%= link_to raw('<em class="fa fa-trash"></em>'), cardtool_path(action: :delete,id: cardtool.id),method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger" %>

                        </td>
						<td><%= cardtool.title %></td>
						<td><%= cardtool.description %></td>
					</tr>
					<% end %>
					<% end %>
                        </tbody>
                </table>
            
              </div>
              </div>
	</div>




	<div class="col-md-4 panel-default panel">
		<h3>Creating a Campaign</h3>
		<ol>
			<li>Click "Add Campaign" in order to start testing.</li>
			<li>Add the name of your campaign and a quick description.</li>
			<li>Build your information architecture that you would like to test.</li>
			<li>Send out the survey to users for testing!</li>
			<li>Look at the results and change the architecture as you wish.</li>
		</ol>
		</p>
		<h3>More Features Coming soon!</h3>
		<p>We'd like to thank you for using our tree testing tool. Know that more features and tools are coming in the very near future.</p>

	</div>
</div>


</div>